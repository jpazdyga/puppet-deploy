---
:backends:
  - yaml
  - json
:yaml:
  :datadir: /etc/puppet/environments/%{::environment}/hieradata
:json
  :datadir: /etc/puppet/environments/%{::environment}/hieradata
:hierarchy:
  - "node/%{::role}"
  - "node/%{::environment}"
  - "node/%{::fqdn}"
  - common
